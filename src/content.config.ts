/**
 * Productos and colecciones moved to the Turso DB (see src/lib/products.ts,
 * src/lib/colecciones.ts, and migrations/0002_products.sql). This file is
 * intentionally minimal; restore collection definitions here if any new
 * markdown-backed content is added.
 */
export const collections = {};
